#include <bits/stdc++.h>
using namespace std;

int main()
{

    // list<int> my_list = {10, 20, 40, 20, 30, 60, 50};
    // my_list.remove(20);
    // for (int value : my_list)
    // {
    //     cout << value << " ";
    // }

    // list<int> my_list = {10, 20, 40, 20, 30, 60, 50};
    // // my_list.sort();//assending order.
    // my_list.sort(greater<int>());//decending order.
    // for (int value : my_list)
    // {
    //     cout << value << " ";
    // }

    // list<int> my_list = {10, 20, 40, 10, 40, 10, 20, 30, 60, 50};
    // my_list.sort();
    // // my_list.sort(greater<int>());
    // my_list.unique();
    // for (int value : my_list)
    // {
    //     cout << value << " ";
    // }

    // list<int> my_list = {10, 20, 40, 10, 40, 10, 20, 30, 60, 50};

    // my_list.reverse();
    // for (int value : my_list)
    // {
    //     cout << value << " ";
    // }

    // element acceses.
    list<int> my_list = {10, 20, 40, 30, 60, 50};
    // cout << my_list.back();  // tail value print.
    cout << my_list.front(); // head value print.
    // cout << *next(my_list.begin(), 3);
    return 0;
}
