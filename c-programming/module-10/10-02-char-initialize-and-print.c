#include <stdio.h>
#include <string.h>
#include <math.h>

int main()
{

    // char array[8] = {'S', 'h', 'a', 'r', 'm', 'i', 'n'};

    char array[8] = "Sharmin"; // initialize power
    printf("%d\n", sizeof(array));

    printf("%s", array); // print power

    return 0;
}