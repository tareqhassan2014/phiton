    }
};

main()
{
    // myQueue q;
    // q.push(10);
    // q.push(20);
    // q.push(30);

    // cout << q.front() << endl;

    // q.pop();
    // cout << q.front() << endl;

    // q.pop();
    // cout << q.front() << endl;

    // if (!q.empty())
    // {
    //     cout << q.front() << endl;
    // }
    myQueue q;
    int input_number;
    cin >> input_number;

    for (int i = 0; i < input_number; i++)
    {