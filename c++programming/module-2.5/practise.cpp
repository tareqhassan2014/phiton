#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int n;
    // cin >> n;

    // int ar[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> ar[i];
    // }

    // int mx = ar[0];

    // for (int i = 0; i < n; i++)
    // {
    //     mx = max(ar[i], mx);
    // }

    // cout << mx;

    int a, b;
    cin >> a >> b;

    cout << a << " + " << b << " = " << a + b << endl;
    cout << a << " * " << b << " = " << static_cast<long long>(a) * b << endl;
    cout << a << " - " << b << " = " << a - b << endl;
    return 0;
}