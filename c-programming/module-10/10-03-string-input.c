#include <stdio.h>
#include <string.h>
#include <math.h>

int main()
{

    char array[8] = "Sharmin"; // initialize power
    scanf("%s", array);

    printf("%s\n", array); // print power

    return 0;
}