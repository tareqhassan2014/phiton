#include <bits/stdc++.h>
using namespace std;

int main()
{

    // string s, x;

    // cin >> s >> x;

    // int count = 0;
    // while (s.find("x") != 1)
    // {
    //     count++;
    // }
    // cout << count;

    return 0;
}